# API externa base (ex: JSONPlaceholder, outros serviços REST)
API_BASE_URL=https://jsonplaceholder.typicode.com

# Logging level (INFO, DEBUG, WARNING, ERROR)
LOG_LEVEL=INFO

# Retry configuration for external calls
RETRY_ATTEMPTS=3
CIRCUIT_BREAKER_TIMEOUT=30

# Ambiente (local, dev, staging, prod)
ENVIRONMENT=local

# Ajuda o VSCode e o Python a resolver imports relativos
PYTHONPATH=.
